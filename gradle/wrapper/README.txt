The binary `gradle-wrapper.jar` is intentionally not included here.

It is generated automatically the first time you:
  - open this project in Android Studio (it runs a Gradle sync), OR
  - run `gradle wrapper --gradle-version 8.7` from this directory
    (requires a system Gradle install).

After that, `./gradlew` works from the command line.
